# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# testing
/coverage

# next.js (.next-deploy = getrenntes Deploy-distDir, siehe netlify.toml)
/.next/
/.next-deploy/
/.next-dev.log
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (KEINE Secrets committen!)
.env
.env.local
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# supabase (lokale CLI-Artefakte; Migrations/Seed bleiben getrackt)
supabase/.branches
supabase/.temp

# Local Netlify folder
.netlify
tailwindcss-*.log

# Claude Code lokale Einstellungen (maschinenspezifisch)
.claude/

dev.log
prod.log

# Lokale Stresstest-Skripte
.stress-*.mjs

# Lokales DB-Hilfsskript (liest .env.local)
.dsgvo-run.cjs

# Word-Fassungen fuer den Mandanten — werden aus docs/datenschutz/*.md
# erzeugt (dort liegt die gepflegte Quelle). Binaerdateien gehoeren nicht
# in die Historie.
/Unterlagen für Go Live/

# Lokale Screenshot-/Verifikationsskripte
.shot-*.mjs
